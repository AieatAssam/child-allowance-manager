<MudDialog>
    <TitleContent>Add Funds</TitleContent>
    <DialogContent>
        <MudForm @ref="_form">
            <MudNumericField Label="Amount"
                             @bind-Value="Amount"
                             Min="0.1m"
                             Required="true"
                             InputMode="@(InputMode.@decimal)"
                             Immediate="true"/>
            <MudText Typo="Typo.caption">@($"{Child.Balance:C2} + {Amount:C2} = {Child.Balance + Amount:C2}")</MudText>
            <MudTooltip Text="Round Up">
                <MudIconButton Icon="@Icons.Material.Filled.ArrowCircleUp"
                               aria-label="Round up amount"
                               Color="Color.Success"
                               OnClick="RoundUp"></MudIconButton>
            </MudTooltip>
            <MudTextField Label="Description"
                          @bind-Value="Description"
                          Required="true"/>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="AddFunds" Disabled="@IsBusy" Color="Color.Primary">Add</MudButton>
        <MudButton OnClick="MudDialog.Cancel" Disabled="@IsBusy" Color="Color.Secondary">Cancel</MudButton>
    </DialogActions>
    @if (IsBusy) { <MudProgressLinear Indeterminate="true" Class="mt-2" aria-label="Saving" /> }
</MudDialog>
